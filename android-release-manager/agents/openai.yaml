interface:
  display_name: "Android Release Manager"
  short_description: "Ship signed Android bundles through GitHub and Play"
  default_prompt: "Use $android-release-manager to build, verify, sign, and deliver this Android release safely."
policy:
  allow_implicit_invocation: true
