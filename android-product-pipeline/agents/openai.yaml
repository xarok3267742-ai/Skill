interface:
  display_name: "Android Product Pipeline"
  short_description: "Build and release Android products end to end"
  default_prompt: "Use $android-product-pipeline to take my Android product from discovery through a verified Google Play release."
policy:
  allow_implicit_invocation: true
